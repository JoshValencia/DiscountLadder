query RunInput {
  cart {
    lines {
      id
      quantity
      attribute(key: "_tiers") {
        key
        value
      }
      cost {
        totalAmount {
          amount
        }
      }
    }
  }
}
